// Settings page — workspace settings, API keys, and webhook configuration
export default function SettingsPage() {
  return (
    <main>
      <h1>Workspace Settings</h1>
      <p>Configure workspace settings, API keys, webhooks, and SSO.</p>
    </main>
  );
}
